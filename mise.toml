[tools]
java = "temurin-21"

[tasks.build]
description = "Build the executable jar and the distribution zip"
run = "./gradlew dist"

[tasks.test]
description = "Run all tests, the ones against postgres need docker"
run = "./gradlew test"
